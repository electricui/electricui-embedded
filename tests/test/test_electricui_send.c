#include "../../src/electricui.h"
#include "unity.h"
#include "unity_fixture.h"

TEST_GROUP( MessageSend );

TEST_SETUP( MessageSend )
{
    //run before each test

}

TEST_TEAR_DOWN( MessageSend )
{
    //run after each test

}

TEST( MessageSend, send_tracked )
{
    TEST_IGNORE_MESSAGE("TODO: Ensure tracked sending works properly");
}

TEST( MessageSend, send_tracked_large )
{
    TEST_IGNORE_MESSAGE("TODO: Ensure large packet sends work properly");
}

TEST( MessageSend, send_tracked_range )
{
    TEST_IGNORE_MESSAGE("TODO: Range tests for specific sub-sets of data");
}

TEST( MessageSend, send_tracked_range_large )
{
    TEST_IGNORE_MESSAGE("TODO: Range tests for large spans of data");
}

TEST( MessageSend, send_message )
{
    TEST_IGNORE_MESSAGE("TODO: Ensure variable from object pointer works properly");
}
